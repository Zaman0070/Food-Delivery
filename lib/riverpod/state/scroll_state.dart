abstract class ScrollState {
  const ScrollState();
}

class ScrollInitialState extends ScrollState {
  const ScrollInitialState();
}

class ScrollReachedBottomState extends ScrollState {
  const ScrollReachedBottomState();
}
